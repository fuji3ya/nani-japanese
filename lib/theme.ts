// LINE Seed JP — app's main typeface (loaded in app/_layout via expo-font).
// Covers Latin + Japanese, so one family for everything.
export const FONT = {
  regular: 'LINESeedJP_400Regular',
  bold: 'LINESeedJP_700Bold',
  heavy: 'LINESeedJP_800ExtraBold',
} as const;
